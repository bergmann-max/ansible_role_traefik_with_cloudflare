---
# roles/ansible_role_traefik_with_cloudflare/meta/main.yml

galaxy_info:
  author: Max Bergmann
  description: >
    Ansible role to deploy Traefik with Docker and automatic HTTPS using
    Cloudflare DNS challenge – based on environment variables for API token.
  license: Unlicense
  min_ansible_version: "2.11"
  platforms:
    - name: Ubuntu
      versions:
        - all
  galaxy_tags:
    - traefik
    - docker
    - cloudflare
    - reverseproxy
    - acme
    - dns
    - https

dependencies: []
