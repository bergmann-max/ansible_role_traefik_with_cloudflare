# ---------------------------------------------
# 🛠️ .env Configuration File for traefik Setup
# ---------------------------------------------
#
# This file contains sensitive environment variables
# used by the Docker Compose setup.
#
# ⚠️ IMPORTANT:
# 1. Copy the `example.env` file to `.env`:
#    cp example.env .env
#
# 2. Fill in all required values.
#    Do NOT leave any fields blank or use default values
#    in production environments for passwords.
#
# 3. Make sure this `.env` file is NOT committed to Git.
#    Add it to your `.gitignore`.

CF_DNS_API_TOKEN=foo-bar-barz-qux